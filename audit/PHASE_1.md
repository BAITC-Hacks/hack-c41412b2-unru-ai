# PHASE 1 — проверяемое ядро

23.09.2026. Область checkpoint по запросу участника: только три валидных CSV, тесты правил и одна команда запуска. UI, AI, temporal не входят в этот этап.

## Уточнение PHASE 0: 377 vs 354

Независимо пересчитано по суммам edges и по агрегации transactions.

| Условие | out>in | out−in>0.01 KZT |
|---|---:|---:|
| Все | 377 | 377 |
| seed | 42 | 42 |
| non-seed | 335 | 335 |
| in>0 | 354 | 354 |
| in=0 | 23 | 23 |

23 узла с нулевым входом — все seed. Таким образом, число 354 воспроизводится при условии положительного входа, а не при исключении seed. Это установленное соответствие, не подтверждение методики автора ТЗ. Нет необходимости изменять данные или подгонять алгоритм. Полный граф: 16 компонент из рёбер + 19 изолятов = 35.

## Переприоритизация

1. Engine, CSV, правила, тесты и запуск — текущий checkpoint.
2. Наблюдаемость/очереди, UI с поиском всех gid.
3. Минимальный AI: get_node_metrics, compare_nodes, get_node_neighbors, get_data_gaps.
4. Temporal — только после работающих пунктов выше и при запасе времени.

В 17:00–17:15 остановить новые функции; оставшееся время — проверка воспроизводимости, README, сдача. Это внутренний план при официальном завершении в 18:00, не автоматическое расписание.

## Контракт checkpoint

Реальный pipeline запускается `python -m moneygraph.pipeline`, валидирует входные пары/суммы/количества в тиынах и выходные схемы; сохраняет исходные 2248 gid. Силы правил раскрыты в README и отдельными колонками. Изоляты и граница не исчезают. Слабые признаки не подменяются уверенной классификацией.

Актуальные измерения и хеши — в `outputs/run_report.json`. CSV — в `outputs/`, не старые пустые файлы starter. `.env` проверен через `git check-ignore`, игнорируется и не отслеживается; содержимое не выводилось. API-вызовов нет. Замена ранее показанного ключа отложена пользователем.

## Проверка checkpoint

- 14 тестов прошли в отдельном новом virtualenv с установкой requirements-dev.txt (uv pip, macOS arm64, Python 3.12.14).
- Три CSV в чистом окружении совпали байт-в-байт с результатом разработки.
- Первый расчёт в основном окружении: 0.3113 с; первый расчёт в чистом окружении: 3.6147 с (внутренний таймер без начального импорта библиотек).
- 2248 узлов; 91 кластер; top-50; максимальная длина evidence 114 символов.
- Роли: peripheral 1871, consolidator 110, distributor 96, transit 62, terminal 56, coordinator 53. Это результат эвристик, не ground truth.
- 444 граничных узла: terminal=0. 19 изолятов сохранены.

Большие gid (около 10^17) превышают безопасный целочисленный диапазон JavaScript. В следующем этапе UI/API передавать идентификаторы строками, не JS Number; текущие CSV сохраняют точные int64.
